---
sidebar: sidebar 
permalink: br-start-discover-oracle.html 
keywords: backing up, back up, backup, backup application data, Amazon Web services, AWS, NetApp Backup and Recovery, workloads, protect, Oracle 
summary: NetApp Backup and Recovery는 워크로드를 보호하기 전에 먼저 워크로드를 검색해야 합니다. 
---
= NetApp Backup and Recovery에서 Oracle Database 워크로드를 검색하세요
:hardbreaks:
:allow-uri-read: 
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ./media/


[role="lead"]
NetApp Backup and Recovery에서 Oracle Database 워크로드를 검색하여 백업 및 스냅샷으로 보호할 수 있습니다.

*필수 NetApp Console 역할* 백업 및 복구 슈퍼 관리자. 자세히 알아보기 link:reference-roles.html["백업 및 복구 역할 및 권한"]. https://docs.netapp.com/us-en/console-setup-admin/reference-iam-predefined-roles.html["모든 서비스에 대한 NetApp Console 액세스 역할에 대해 알아보세요."^].



[[configure-dns-for-air-gapped-host-discovery-in-netapp-console-local-deployment]]
== NetApp Console 로컬 배포에서 에어갭 호스트 검색을 위한 DNS 구성

NetApp Console 로컬 배포를 표준 DNS를 통해 데이터베이스 호스트에 접근할 수 없는 에어갭 환경에서 사용하는 경우, 내부 RKE2 CoreDNS 구성을 일시적으로 수정해야 합니다. 이렇게 하면 노드의 `/etc/hosts`에 항목을 추가하는 것과 유사하게 Pod가 클러스터 내의 특정 호스트 이름을 IP 주소로 확인할 수 있습니다.


IMPORTANT: 변경하기 전에, 기존의 사용자 지정을 덮어쓰지 않도록 기존 CoreDNS 구성과 기존 `rke2-coredns-config.yaml` HelmChartConfig를 모두 확인하십시오. 대부분의 NetApp Console 로컬 배포 노드에는 이 파일이 이미 존재하며 리소스 요청이 이미 포함되어 있습니다. 파일이 이미 존재하는 경우 파일을 다시 생성하지 말고, 대신 기존 `valuesContent`에  `servers:` 블록만 추가하십시오. RKE2 operator 및 runtime에 의해 일관되게 계속 관리되도록 RKE2 manifest를 사용하여 이 변경 사항을 적용하십시오.

.시작하기 전에
* 가능한 한 HelmChartConfig 파일을 변경 관리 대상에 포함시키십시오.
* CoreDNS 재정의의 목적이 명확하도록 파일 이름을 알아보기 쉽게 지정하십시오.
* Corefile에서 예상되는 DNS 레코드가 보이지 않으면 RKE2 서버 로그를 확인하고 매니페스트 파일의 구문을 확인하십시오.


.단계
. 기존 CoreDNS 구성을 확인하여 기존 변경 사항을 덮어쓰지 않도록 하십시오.
+
[source, shell]
----
kubectl -n kube-system get cm rke2-coredns-rke2-coredns -o jsonpath='{.data.Corefile}'
----
. HelmChartConfig 파일이 존재하는지 확인하십시오.
+
[source, shell]
----
sudo cat /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml
----
+
[role="tabbed-block"]
====
.파일이 존재하는 경우
--
.. 다음 스크립트를 실행하여 기존 HelmChartConfig 파일에  `servers:` 블록을 추가하십시오.
+
[source, shell]
----
MANIFEST=/var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml

# 1. Back up first
sudo cp -a "$MANIFEST" "$MANIFEST.bak.$(date +%Y%m%d-%H%M%S)"

# 2. Safety checks: valuesContent must be the last key, no servers: block yet
grep -n 'servers:' "$MANIFEST"        # must return nothing
tail -c 1 "$MANIFEST" | od -c | head  # file must end with a newline

# 3. Append ONLY the servers block (nothing above it changes)
sudo tee -a "$MANIFEST" >/dev/null <<'YAML'
    servers:
    - zones:
      - zone: .
      port: 53
      plugins:
      - name: errors
      - name: health
        configBlock: |-
          lameduck 10s
      - name: ready
      - name: kubernetes
        parameters: cluster.local in-addr.arpa ip6.arpa
        configBlock: |-
          pods insecure
          fallthrough in-addr.arpa ip6.arpa
          ttl 30
      # ───────────────────────── EDIT BELOW ─────────────────────────
      # Static DNS records for hosts the corporate DNS cannot resolve.
      # Format:  <IP>  <FQDN>  [optional aliases...]
      # `fallthrough` MUST stay as the last line -- it lets every other
      # name go to the upstream resolver via the `forward` plugin below.
      - name: hosts
        configBlock: |-
          1.2.3.4 example.domain.com exampleshortname
          fallthrough
      # ───────────────────────── EDIT ABOVE ─────────────────────────
      - name: prometheus
        parameters: 0.0.0.0:9153
      - name: forward
        parameters: . /etc/resolv.conf
      - name: cache
        parameters: 30
      - name: loop
      - name: reload
      - name: loadbalance
YAML

# 4. Confirm the merged result
sudo cat "$MANIFEST"
----
.. HelmChartConfig 파일에  `servers:` 블록을 추가한 것 외에 다른 변경 사항이 없는지 확인하십시오.


--
.파일이 존재하지 않는 경우
--
.. 다음 스크립트의  `configBlock` 영역을 편집하고 정적 DNS 레코드를 추가하십시오. 이렇게 하면 RKE2 매니페스트 폴더에 HelmChartConfig YAML 파일이 생성되며, RKE2가 자동으로 이 파일을 적용하여 CoreDNS 구성을 재정의합니다.
+
[source, shell]
----
sudo tee /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml >/dev/null <<'YAML'
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: rke2-coredns
  namespace: kube-system
spec:
  # NOTE: This is a HelmChartConfig that *overlays* values onto the rke2-coredns
  # HelmChart already managed by RKE2. RKE2 watches /var/lib/rancher/rke2/server/manifests
  # and will re-render the rke2-coredns Helm release whenever this file changes.
  #
  # The `servers:` list below REPLACES the chart's default server definition,
  # so every plugin that was in the stock Corefile must be present here.
  # Do NOT delete plugins you don't recognize -- they are required for cluster DNS.
  valuesContent: |-
    servers:
    - zones:
      - zone: .
      port: 53
      plugins:
      - name: errors
      - name: health
        configBlock: |-
          lameduck 10s
      - name: ready
      - name: kubernetes
        parameters: cluster.local in-addr.arpa ip6.arpa
        configBlock: |-
          pods insecure
          fallthrough in-addr.arpa ip6.arpa
          ttl 30
      # ───────────────────────── EDIT BELOW ─────────────────────────
      # Static DNS records for hosts the corporate DNS cannot resolve.
      # Format:  <IP>  <FQDN>  [optional aliases...]
      # `fallthrough` MUST stay as the last line -- it lets every other
      # name go to the upstream resolver via the `forward` plugin below.
      - name: hosts
        configBlock: |-
          1.2.3.4 example.domain.com exampleshortname
          fallthrough
      # ───────────────────────── EDIT ABOVE ─────────────────────────
      - name: prometheus
        parameters: 0.0.0.0:9153
      - name: forward
        parameters: . /etc/resolv.conf
      - name: cache
        parameters: 30
      - name: loop
      - name: reload
      - name: loadbalance
YAML
sudo chmod 600 /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml
----
.. 스크립트를 복사하여 실행합니다.


--
====
. CoreDNS가 변경 사항을 반영했는지 확인하십시오.
+
[source, shell]
----
kubectl -n kube-system get cm rke2-coredns-rke2-coredns -o jsonpath='{.data.Corefile}'
----
+
몇 분 후에도 변경 사항이 보이지 않으면 CoreDNS 배포를 다시 시작한 다음 Corefile을 다시 확인하십시오.

+
[source, shell]
----
kubectl -n kube-system rollout restart deploy/rke2-coredns-rke2-coredns
----
. 데이터베이스 호스트를 찾은 후 항목을 삭제해야 하는 경우 다음 단계를 따르십시오.
+
[role="tabbed-block"]
====
.기존 파일에 내용을 추가한 경우
--
.. 백업된 파일을 복원하거나 파일 내에서 해당 `servers:` 블록만 삭제합니다.
+
[source, shell]
----
sudo cp -a /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml.bak.<timestamp> \
/var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml
----
+
해당 파일에는 기존 RKE2 리소스 튜닝 정보가 포함되어 있으므로 삭제하지 마십시오.

.. 파일을 복원한 후 호스트 항목을 삭제하십시오. 오래된 항목은 호스트 검색 및 예약 백업 문제를 일으킬 수 있습니다.
.. CoreDNS 배포를 다시 시작합니다.
+
[source, shell]
----
kubectl -n kube-system rollout restart deploy/rke2-coredns-rke2-coredns
----


--
.새 파일을 만들었다면
--
.. manifests 폴더에서 HelmChartConfig 파일을 삭제합니다.
+
[source, shell]
----
sudo rm /var/lib/rancher/rke2/server/manifests/rke2-coredns-config.yaml
----
.. 파일을 삭제한 후에는 호스트 항목을 제거하십시오. 오래된 항목은 호스트 검색 및 예약된 백업 문제를 일으킬 수 있습니다.
.. CoreDNS 배포를 다시 시작합니다.
+
[source, shell]
----
kubectl -n kube-system rollout restart deploy/rke2-coredns-rke2-coredns
----


--
====




[[add-an-oracle-database-host-and-discover-resources]]
== Oracle 데이터베이스 호스트를 추가하고 리소스를 검색합니다

Oracle Database 호스트 정보를 추가하고 NetApp Backup and Recovery가 워크로드를 검색하도록 하십시오. 각 콘솔 에이전트에 대해 검색할 시스템을 선택하십시오.

.단계
. NetApp 콘솔 메뉴에서 *보호* > *백업 및 복구*를 선택합니다.
. *워크로드*에서 *Oracle* 타일을 선택하십시오.
. *리소스 검색*을 선택하세요.
. *워크로드 유형* 필드에서 *Oracle*을 선택하십시오.
. 이 Oracle Database 호스트에 대한 자격 증명을 아직 저장하지 않은 경우 *자격 증명 추가*를 선택하십시오.
+
.. 이 호스트와 함께 사용할 콘솔 에이전트를 선택하세요.
.. 이 자격증의 이름을 입력하세요.
.. 계정의 사용자 이름과 비밀번호를 입력하세요.
.. *완료*를 선택하세요.


. *호스트 등록*: 새 Oracle Database 호스트를 추가합니다. *호스트 FQDN 또는 IP 주소* 필드에 호스트의 FQDN 또는 IP 주소를 입력합니다. 클러스터형 데이터베이스의 경우 클러스터의 모든 노드의 FQDN 또는 IP 주소를 입력할 수 있습니다. 그런 다음 자격 증명, Console 에이전트 및 포트 번호를 제공합니다.
. (선택 사항) 루트 사용자가 아닌 사용자의 자격 증명을 선택하는 경우 다음을 수행하십시오.
+
.. Oracle Database 호스트의 sudoers 파일에 선택한 비루트 사용자를 추가하는 방법에 대한 지침을 보려면 *sudoers 구성*을 선택하십시오.
.. 대화 상자의 지침을 따르고 완료되면 확인란을 활성화한 다음 *완료*를 선택하십시오.


. *고급 설정*: 다음을 수행하십시오:
+
.. NetApp 플러그인에 사용할 포트와 설치 경로를 입력하십시오. 이 플러그인을 사용하면 Oracle 데이터베이스 호스트와 NetApp Backup and Recovery 간의 통신이 가능해집니다.
.. NetApp Backup and Recovery에서 각 호스트에 플러그인을 자동으로 설치하도록 허용할지, 아니면 모든 호스트에 대한 자동 플러그인 설치를 건너뛸지 선택하십시오. 수동 설치 지침을 보려면 *방법 보기*를 선택하십시오.
+
[NOTE]
====
NetApp Backup and Recovery는 SSH를 통해 각 호스트에 연결하여 플러그인을 자동으로 설치합니다. 다음 중 하나에 해당하는 경우 *수동 설치 사용*을 활성화하십시오.

*** 하나 이상의 호스트에서 SSH 서비스가 실행되고 있지 않습니다.
*** 모든 호스트에 NetApp 플러그인이 이미 설치되어 있습니다(클러스터 멤버 중 일부에만 설치되어 있는 경우에도 마찬가지입니다).
*** 각 호스트에 플러그인을 수동으로 설치하는 것을 선호합니다.


====
.. 데이터베이스 호스트가 클러스터링된 경우 *클러스터의 모든 호스트 추가* 옵션을 활성화하여 클러스터의 모든 호스트를 검색합니다.
.. 플러그인을 자동으로 설치하기 전에 각 호스트에서 사전 설치 검사를 실행할지 여부를 선택하십시오. 검사에 실패하면 자동 플러그인 설치가 중지됩니다. 검사를 건너뛰고 설치하려면 *선택적 사전 설치 검사 건너뛰기*를 활성화하십시오(수동 설치를 선택하면 자동으로 활성화됩니다).


. *발견*을 선택하세요.
+

TIP: 리소스를 추가하는 데 몇 분 정도 소요될 수 있습니다. 진행 상황을 확인하려면 인벤토리 페이지 하단의 상태 대화 상자에서 *진행 상황 추적*을 선택하거나 왼쪽 탐색 메뉴에서 *모니터링*을 선택하십시오.



.결과
Oracle Database 워크로드는 인벤토리 페이지의 워크로드 목록에 나타납니다.



[[continue-to-the-netapp-backup-and-recovery-dashboard]]
== NetApp Backup and Recovery 보드로 계속

. NetApp 콘솔 메뉴에서 *보호* > *백업 및 복구*를 선택합니다.
. 워크로드 타일을 선택하십시오(예: Oracle Database).
. 백업 및 복구 메뉴에서 *대시보드*를 선택합니다.
. 데이터 보호 상태를 검토하십시오. 위험에 처한 워크로드 또는 보호된 워크로드의 수는 새로 발견, 보호 및 백업된 워크로드 수에 따라 증가합니다.

